#include "monty.h"

/**
 * main - Entry point for monty
 * Description: coming soon
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	if (argc != 2 || !(argv))
		errHand(1);
	return (0);
}
